{
    "name": "edulazaro/larascraper",
    "description": "Scraper for Laravel",
    "license": "MIT",
    "type": "library",
    "require": {
        "php": "^8.2",
        "laravel/framework": ">=10.0",
        "symfony/dom-crawler": "^7.2"
    },
    "suggest": {
        "guzzlehttp/guzzle": "Required only for the lightweight 'http' driver (->driver('http')), which scrapes via a plain HTTP request instead of a headless browser."
    },
    "autoload": {
        "psr-4": {
            "EduLazaro\\Larascraper\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "EduLazaro\\Larascraper\\Tests\\": "tests/"
        }
    },
    "authors": [
        {
            "name": "Edu Lazaro",
            "email": "edu@edulazaro.com"
        }
    ],
    "extra": {
        "laravel": {
            "providers": [
                "EduLazaro\\Larascraper\\LarascraperServiceProvider"
            ]
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "orchestra/testbench": "^8.34",
        "guzzlehttp/guzzle": "^7.0"
    }
}
